{
  "name": "@multifeed/oauth-relay",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "check-types": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "lint": "eslint . --max-warnings 0"
  },
  "devDependencies": {
    "@multifeed/eslint-config": "workspace:^",
    "@multifeed/typescript-config": "workspace:^",
    "eslint": "^10.3.0",
    "typescript": "6.0.3",
    "wrangler": "^4.110.0"
  }
}
