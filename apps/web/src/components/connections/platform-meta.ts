/**
 * Re-export shared platform metadata so connections UI and post composer stay in sync.
 */
export {
  CONNECTABLE_PLATFORMS,
  PLATFORM_META,
  platformBrand,
  platformLabel,
  type OAuthPlatform,
  type PlatformKey,
  type PlatformMeta,
} from "@/lib/platform-meta";
